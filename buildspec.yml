version: 0.2

phases:
  install:
    commands:
      - echo "Nothing to install for static site"
  build:
    commands:
      - echo "No build needed for static site"
artifacts:
  files:
    - '**/*'
